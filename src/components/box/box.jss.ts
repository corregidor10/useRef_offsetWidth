import { createUseStyles } from "react-jss";
export const BoxStyles = createUseStyles({
  boxContainer: {
    display: "flex",
    flexDirection: "column",
    borderRadius: 20,
    padding: 16,
    backgroundColor: "white",
    minHeight: 100
  }
});
